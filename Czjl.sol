pragma solidity >=0.4.22 <0.6.0;
//import "LibJson.sol";

contract CzjlContract { //操作记录  
    //using LibJson for *;

    //记录案件信息, 输入案号, 案件标识, 案件信息(json)
    function aj_xxjl(string ah, string ajbs, string ajxx) public returns(bool){
        return true;
    }
}