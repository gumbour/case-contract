pragma solidity >=0.4.22 <0.6.0;
//import "LibJson.sol";

contract CzjlContract { //操作记录  
    //using LibJson for *;

    //记录案件信息, 输入案号, ocr, 案件信息(json)
    function aj_xxjl(string memory ah, string memory ocr, string memory ajxx) public returns(bool){
        return true;
    }
}